web框架基础设施

by hxzon

1，css，js框架

2，css，js处理：压缩，合并，按需引入

3，服务注入

4，数据校验：前端输入校验，后端校验

========
5，请求处理（提取参数）

5.1，url路由

5.2，url参数提取（rest）

5.3，参数与模型的绑定
参数类型：上下文参数，组件参数，数据参数

5.4，请求的上下文：
保持会话；
通过上下文参数重建（页面url，表单action url，上下文参数）

========
6，组件化：基于js，css的按需引入，html片段的组合

7，丰富的组件：前段交互（js框架），前后端ajax通信（json params，json response）

8，模板
8.1，方便的动态生成html（缺点是不如模板直观）
8.2，模板语言（如tml，Freemarker）
8.3，模板+动态修改

========
9，集群

